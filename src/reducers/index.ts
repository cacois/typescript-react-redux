import { combineReducers } from 'redux';
import { State } from './../state';

// FIXME: Add reducers

// Global reducer definition - maps reducers to pieces of state
export const reducers = combineReducers<State>({
  // FIXME: add reducer mappings
});
